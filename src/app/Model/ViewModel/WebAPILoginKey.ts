export interface WebAPILoginKey {
    apiCode: number,
    apiAppName: string,
    apiKeyOwner: string,
    keyStatus: boolean,
    apiKey: string,
    createDate: Date,
    Username?: string;
    Password?: string;
}